import { TransactionTypeSignPipe } from './transaction-type-sign.pipe';

describe('TransactionTypeSignPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionTypeSignPipe();
    expect(pipe).toBeTruthy();
  });
});
